# Service-change-aware asynchronous FL manuscript structure

> 文档定位：聚合服务变化版本的 OSDI 论文结构稿。本文档组织论文叙事、系统章节、评价假设和证明篇幅；它不是论文初稿。

## 1. Editorial decision

论文的中心对象是一项持续运行的异步联邦学习系统。客户端以不同速度提交更新，模型持续推进，承担聚合和更新保护的服务节点在训练过程中加入、退出、崩溃并恢复。论文研究的系统问题是：

> 当异步联邦学习的聚合服务发生变化时，如何同时保持在途训练的连续性与已发布聚合结果的一致性？

动态成员在这里决定训练状态如何跨越聚合服务变化继续推进，以及已经发布的聚合结果如何退出后续计算路径。论文的主要贡献是建立异步聚合在服务更替下的延续语义，使训练连续性和结果最终性在同一系统中同时成立。

安全模型和证明支撑这项系统保证。论文采用受限移动敌手模型：敌手可以跨共享周期自适应选择腐蚀对象，每个周期限制累计暴露的不同节点数；活动实例在服务更替时刷新份额，随后清除旧份额。该模型放入系统假设，贡献围绕训练连续性、状态延续和历史更新保护展开。

## 2. Introduction route

### Paragraph 1: the running service

从一个持续运行的 FL 系统开始。客户端在线时间、计算速度和网络延迟各不相同，系统以聚合实例为单位异步推进模型，而不是等待固定轮次中的所有客户端。模型服务器需要在客户端持续到达时发布更新。

### Paragraph 2: service change during training

服务运行期间，负责聚合和恢复的服务节点也会变化。节点可能离开、崩溃或重新加入，新的服务组需要继续处理后续客户端。成员变化与客户端到达同时发生，训练服务不能把它们分成两个互不相干的阶段。

### Paragraph 3: the missing service-change semantics

FedBuff 和 Buffalo 建立了缓冲式异步聚合路径，LightSecAgg 通过编码恢复降低掉线处理成本，Flamingo 进一步支持解密委员会的周期性更换。本文沿这些工作研究持续训练中的服务变化：一个尚未形成最终参与集合的聚合，如何在旧服务退出时保留已接收更新、转交继续计算所需的能力，并与其他聚合一起推进模型。委员会轮换、恢复冗余和异步缓冲由此成为方案基础。

### Core question

因此，本文提出核心问题：

> **当异步联邦学习的聚合服务发生变化时，如何保持在途训练连续推进，并使已发布聚合结果具有最终性？**




### Challenge 1: service changes interrupt asynchronous aggregation

客户端按各自速度完成训练，一次聚合可能在服务更替时已经接收部分更新，还在等待其余更新。新服务需要知道哪些计算已经纳入，并取得继续聚合所需的密文和秘密份额。FedBuff 和 Buffalo 提供缓冲式异步聚合；Flamingo 进一步支持解密委员会的周期性更换。这些机制为本文提供基础，本文继续研究在途聚合与服务更替交错执行时，如何同时保持输入归属、计算进度和模型应用顺序。
这会产生一个具体的训练错误：同一更新可能在原服务和新服务分别获得接受路径，或者因为无法判断归属而被丢弃。前者改变聚合输入，后者损失已经完成的客户端计算。两种结果都会破坏异步训练中的模型推进和结果可复现性。


### Our solution to challenge 1

本文把聚合实例作为服务更替的逻辑单位。原服务确认已经接受的更新前缀，后继服务从该前缀继续计算，并继承同一个聚合实例的逻辑身份。服务者发生变化，训练输入和模型版本保持连续；结果确定后，实例转化为只读的训练事实。

### Challenge 2: state continuity conflicts with result finality

节点退出或崩溃会使状态延续消息延迟、重复或丢失。持续迁移全部可恢复状态能够保留计算进度，却会把旧聚合的份额、修复材料和恢复记录带入后续服务。周期性重建可以缩短状态的存续范围，却会让新训练等待转换完成，或迫使未完成更新重新计算。现有动态状态方案围绕长期秘密的连续使用组织恢复，因而适合维持可恢复能力，却没有区分活动训练状态与已经发布的聚合结果。

这形成了第二个具体挑战：系统需要继续迁移尚未完成的计算，又要让已经发布的结果退出后续恢复路径。单独延续状态会留下历史聚合的访问能力，单独切断状态会牺牲训练进度。
### Our solution to challenge 2

本文设计带最终性边界的状态延续。活动聚合携带继续计算所需的状态迁移到后继服务；结果确定后，聚合状态转化为只读结果事实。后继服务据此继续推进活动训练，同时沿着最终性边界读取已发布结果。成员变化带来的代价可以直接体现在服务切换延迟、客户端纳入率和模型推进速度上。

### Running scenario: one interrupted aggregation instance

用一个尚未完成的聚合实例贯穿协议和实验。原聚合服务已经接受了部分客户端更新，但实例尚未达到发布门槛；其中一个客户端已经完成本地训练，更新仍在网络中传递。此时原服务发生节点退出或崩溃，新的服务组开始工作，迟到更新与状态延续消息交错到达。

这个场景同时检验两件事。首先，在状态可用性条件成立时，后继服务是否能够沿用原服务已经确认纳入的训练输入，继续完成同一个聚合实例。尚在客户端或网络中的更新通过重传申请纳入，其最终交付还取决于客户端和通信可用性。其次，当该实例发布结果后，迟到的旧消息和后续恢复操作是否仍然能够改变参与集合、结果或开启路径。

本文的机制在这个场景中保持一条清晰的逻辑链：原服务确认已接受的输入前缀，后继服务继承该聚合实例并继续接收合格的后缀；结果发布后，实例只保留结果事实，后继服务沿最终性边界处理所有迟到消息。这个场景把“训练连续性”和“结果最终性”连接为一个系统问题。

### Comparison anchor: direct composition

论文需要把本文与一种最有说服力的直接组合方案分开。该方案由异步安全聚合、标准可验证重分享和每个聚合实例完成后的密钥删除组成，但没有规定聚合实例如何跨服务更替保持身份和输入边界。

| 方案 | 在途更新的处理 | 已发布结果的处理 | 主要代价或缺口 |
|---|---|---|---|
| 固定聚合服务 | 服务节点不变，实例继续计算 | 按原协议完成 | 没有服务变化能力 |
| 直接组合 | 延续聚合状态或等待原服务排空 | 完成后删除实例密钥 | 需要在丢弃未完成工作与延续旧状态之间选择，缺少统一的实例归属语义 |
| 周期性重建 | 暂停或排空当前实例后重建 | 以重建边界切断旧状态 | 训练等待和客户端重算增加 |
| 本文方案 | 只延续活动实例，从已确认输入前缀继续计算 | 结果确定后转为只读事实并退出恢复路径 | 付出活动实例状态延续和状态刷新成本 |

直接组合方案不必被表述为密码学上必然不安全。它的关键缺口是没有同时规定在途训练的归属和已发布结果的后续访问边界，因此不同实现会在更新丢弃、状态延续和训练暂停之间作出不同选择。本文的比较重点是：在同一成员变化和消息轨迹下，哪种方案能够保留更多已完成计算，并以明确的最终性边界结束聚合实例。

### Contributions

1. **A service-change semantics for asynchronous FL.** 提出聚合服务变化下的异步联邦学习问题，明确在途更新的归属、模型版本的连续推进和已发布聚合结果的最终性。
2. **Finality constrained state continuation.** 设计以聚合实例为单位的状态延续机制，使活动训练可以跨服务更替继续推进，并使已经确定的结果转化为后继服务可读取的只读事实。
3. **A complete asynchronous FL system.** 将该机制接入缓冲式异步训练和安全聚合路径，处理服务节点加入、退出、崩溃恢复及多个并发聚合实例，并分别测量训练推进、状态延续和保护操作的代价。
4. **An evaluation of continuous training under service changes.** 在相同客户端到达、网络延迟和成员变化轨迹下，与固定服务、全量状态迁移和周期性重建进行比较，评估模型推进、客户端纳入、训练质量、切换代价和结果稳定性。

贡献与评价指标一一对应：第一项由更新归属一致性、模型版本连续性和结果最终性检验；第二项由已完成客户端工作保留率、聚合实例完成率和迟到消息处理结果检验；第三项由服务切换延迟、恢复延迟、通信量和持久状态大小检验；第四项由模型推进速度、客户端纳入率和模型质量检验。

## 3. Main-text order

### 3.1 Setting and service model

本节固定部署方式、训练对象和设计目标，正文预算约一页至一页半。以下内容作为后续撰写依据；证明推导安排在分析章节。

#### 3.1.1 Participants and training

采用**由可变聚合服务提供安全聚合的异步联邦学习**。客户端保留本地数据，从模型服务取得模型，在本地执行若干步 SGD，再异步提交受保护的模型增量。模型服务负责任务分配、保存公开模型以及发布聚合后的模型版本；聚合服务共同维护受保护聚合状态，并通过阈值操作释放聚合结果。模型发布按经确认的结果顺序执行，每个实例的结果应用一次。

模型服务是一个逻辑角色，可以由聚合服务节点承载或复制部署。更新机密性依赖阈值保护；模型发布者可见的学习信息是公开模型和聚合结果。聚合结果的有效性及应用顺序由经认证的服务记录决定，模型服务故障后可由接替者从这些记录继续发布。这一组织方式保留标准的全局模型与缓冲聚合训练流程。

用 $sid$ 标识一次聚合实例，用 $v_{sid}$ 表示其基础模型版本，用 $A_{sid}$ 表示已纳入的更新集合。这三个对象足以描述训练进度。多个实例可以基于不同模型版本并发执行；客户端提交携带更新标识、身份、$sid$ 和基础模型版本，聚合服务为已接收更新生成可核验的纳入记录。服务代数只在状态延续时使用。

客户端 $u$ 的本地训练结果记为 $\Delta_u$，样本权重为 $b_u$。实例的逻辑聚合结果及模型更新写为：

$$
Y_{sid}=\frac{\sum_{u\in A_{sid}}b_u\Delta_u}
                   {\sum_{u\in A_{sid}}b_u},\qquad
w_{t+1}=w_t+\eta_t h(t-v_{sid})Y_{sid}.
$$

这里 $t$ 是结果应用前的模型版本，$h$ 是固定的陈旧度权重。首版采用缓冲式加权平均，保护前确定样本权重与编码方式；所选权重和实例基础版本随已接收更新一并保留。服务切换造成的等待通过结果应用时的陈旧度体现。收敛分析所用的有界陈旧度是学习条件，与异步网络假设分别陈述。

服务在纳入 $k$ 个不同的合格客户端后固定一次聚合；每个客户端在一个实例贡献一次。$k$ 约束学习参与集合，开启门槛约束服务节点，二者分别设置。更新重传沿用原标识，服务返回已有的纳入结果。

#### 3.1.2 Service membership and communication

令 $\mathcal A_r$ 表示第 $r$ 个聚合服务组，成员数为 $n_r=|\mathcal A_r|$。$f_r$ 为腐蚀上限，$\tau_r$ 为聚合开启的密码学门限：同一开启授权下，至少收集 $\tau_r$ 个不同成员的有效部分开启，才能恢复聚合结果。客户端缓冲人数 $k$、开启门限和一致协议所需的确认人数各有作用。经认证的服务变更记录确定当前服务组和后继服务组。系统可以经历多次服务更替，相邻服务组允许成员完全不相交；交接期间双方仍需完成各自的通信和状态处理。每个实例沿服务更替链延续，一次只处理一个状态延续。

服务变更记录由满足其自身容错条件的异步一致协议提供。它确定成员变更的顺序、生效依据和发布权限。聚合服务还需要对纳入记录、冻结前缀以及开启或放弃决定取得一致，并确认对应聚合状态可用。实现可复用同一一致协议，批量确认这些实例记录。服务记录回答由谁提供聚合，实例记录回答已完成哪些计算。

服务变更采用原服务一致确认的最终记录作为交接依据，覆盖已提交结果、待完成实例以及模型应用序列。后继安装该记录后承接后续结果的确认权限；原服务继续完成记录指定的重分享和部分开启。已经确定的开启授权保留原持有者、份额代数和最终参与集合。结果确认由当前授权服务执行，秘密运算由相应份额持有者执行，两者通过同一实例记录关联。

网络采用认证异步通信，消息延迟无已知上界，允许重排和重复。持续在线的诚实参与者之间满足最终交付。发送者退出或崩溃后，其尚未交付的消息允许丢失；接收者离线期间也可能错过消息。已经由其他诚实节点保存的材料可以由保存者继续传送。迟到消息按实例记录和份额代数核验。服务更替依据协议证据；超时可触发重试或提出成员变更请求，最终决定依靠一致记录。

纳入确认同时要求记录一致和保护载荷可用。确认者先保存对应密文、权重和验证材料，再对该批次的纳入作出确认；确认人数及保存条件服从所选一致与可用性协议。客户端取得的回执用于证明纳入事实，状态恢复依赖实际保存的载荷。秘密份额由各持有者保管，通过可验证重分享延续。实现与实验分别检查记录确认、载荷取得和秘密份额可用性。

成员变化按加入和退出描述，可用性按在线和暂时失联描述：

| 事件 | 服务处理 | 对已接收更新的影响 |
|---|---|---|
| 客户端加入或退出 | 认证后获得或终止后续提交资格 | 已接收更新保留在原实例 |
| 聚合服务节点加入或退出 | 确认后继服务组，延续活动实例 | 更新及其权重随实例保留 |
| 服务节点暂时失联后恢复 | 读取经认证的当前服务记录和实例记录，再恢复职责 | 按当前实例状态处理 |
| 已退出服务节点返回 | 通过加入请求重新取得服务资格 | 服务权限由后继服务组赋予 |

有序退出期间，原服务节点停止接收新实例，并完成其已有实例所需的剩余职责；各节点在完成所属协议规定的交付与清除条件后退出。突然离开作为可用性损失处理。恢复节点首先清理过期状态；仍在当前服务组内的节点可按恢复协议重新服务，已退出节点按加入流程处理。

恢复时，节点保留当前尚未完成交接所需的接收私钥，取得并验证交接输入后，按组件规定完成刷新和清除。此时接收私钥仍属于有效恢复材料，其泄露计入相关交接的敌手视图。若该私钥永久丢失，已有密文副本需要配合重分享组件的内部恢复才能继续使用；更换身份或生成新密钥本身无法解开原消息。

训练推进按实例陈述：源服务能够确认并交付冻结前缀，源服务与后继服务满足重分享协议的参与条件，后继服务取得完整状态，并满足旧材料清除与激活条件。随后还需有足够的合格客户端更新到达，以及足够的成员完成聚合开启和结果确认。所需参与者持续服务到完成各自职责，通信与本地执行受到公平调度；采用随机异步一致协议时，终止结论沿用该协议的概率保证。每一时刻的在线人数充足仍可能伴随关键消息反复丢失，因此人数统计需与这些实例条件一起使用。

#### 3.1.3 Restricted mobile adversary

正文统一称为**受限移动敌手**。敌手可以观察公开结果和消息调度，自适应腐蚀聚合服务节点，读取其状态并控制其行为，随后选择其他节点。释放节点后，已读取的信息继续保留在敌手视图中。

对实例 $sid$ 的第 $g$ 代份额，令 $\mathcal D_{sid,g}$ 表示从该代材料生成到旧材料清除期间曾暴露该代份额或恢复材料的不同节点集合。要求：

$$
|\mathcal D_{sid,g}|\le f_r<\tau_r,\qquad g\text{ 由 }\mathcal A_r\text{ 持有}.
$$

上限约束一个共享周期内的累计暴露人数。新服务组开始工作时，旧材料的暴露统计继续有效，直到其清除条件满足；同一次腐蚀若读到两代材料，分别计入两代。状态延续转录和新旧份额的联合视图还须满足所选主动重分享协议的移动安全条件。服务认证与一致协议的长期密钥同样须适应该敌手模型。

隐私依赖认证、阈值保护、可验证重分享和诚实节点安全清除。节点退出记录、安装确认和多数回执分别表示服务事实；物理清除由执行假设和恢复规则保障。已被敌手复制的材料计入其历史视图。客户端隐私按给定公开参与信息及聚合输出后，对诚实客户端更新的额外泄露来定义；与恶意客户端合谋时，已知输入和聚合输出本身能推出的信息属于允许泄露。

允许的聚合输出包含经过整体开启授权、且已有足够有效贡献可恢复的最终聚合。结果进入模型可能稍后发生；隐私分析按实际可恢复的信息计算泄露。开启授权后参与集合固定，实例继续沿该结果完成提交。

系统容错参数和密码学门限分开给出。若所用异步一致组件适用，且导致其无法服务的节点总量由同一故障预算覆盖，可以采用 $n_r\ge 3f_r+1$ 和 $f_r<\tau_r\le n_r-f_r$。额外的诚实节点失联需重新核对该组件的推进条件。实际开启检查：

$$
\bigl|\mathsf{ValidShares}(sid,g,cert)\bigr|\ge \tau_r,
$$

其中只计入同一授权、同一代份额下不同成员的有效贡献；已交付并可取得的贡献在发送者随后退出时仍可使用。冻结确认、重分享、后继安装和结果确认各自遵循其组件条件。本文正文采用这些实例级条件；额外不可用节点数 $\ell_r$ 留在实验与参数讨论中。普通加权平均用于首版训练，分析覆盖纳入一致性、结果完整性和更新机密性，恶意梯度的统计鲁棒性需要另外选用并分析鲁棒聚合算法。

#### 3.1.4 System model and design goals

本文围绕三个设计目标组织后续方案：

1. **Aggregation continuity.** 服务更替保留已经确认的更新前缀及其模型版本，后继服务沿同一实例继续接收合格更新。
2. **Model consistency.** 每个聚合结果只提交并应用一次；提交后的参与集合、聚合值和模型应用位置保持不变。
3. **Privacy across service changes.** 在受限移动敌手和保护组件假设下，单个诚实客户端更新不会因服务更替和份额刷新而额外泄露。

这三个目标分别依赖状态可用性、一致协议和密码学假设。暂时无法延续的实例保留仍可取得的已确认状态并等待恢复；在能够排除重复提交后，可以记录放弃并允许客户端向新实例重提。永久丢失足够的载荷或秘密份额会使该实例无法完成。新服务能够初始化并持续接收合格更新时，可以并行推进其他实例；公平调度避免某个冻结实例阻塞后续训练。条件性推进、结果一致性和更新保护分别在分析章节中给出简短结果，实验报告完成率、等待时间和模型推进。

### 3.2 Reconfigurable asynchronous aggregation

本节用三个部分说明方案：聚合流程、在途聚合的状态延续、结果提交与故障处理。核心机制是：**延续已确认纳入的客户端更新，固定已经提交的聚合结果。**

#### 3.2.1 Aggregation flow

系统围绕聚合实例组织训练、服务变化和更新保护。模型服务分配实例与模型版本；当前聚合服务确认客户端纳入并累加受保护更新；服务变化时，新的服务组可以开始新实例，同时分别延续活动实例。不同实例的交接和结果处理可并行执行，每个实例内部遵守各自的先后关系。结果开启、模型应用和状态清除分别记录。

```text
client trains from w_v and protects its update
    -> current aggregation service admits and accumulates
    -> successor service resumes the same sid after state continuation
    -> service authorizes and opens the final aggregate
    -> model service applies Commit_sid once
    -> holders erase remaining material and record Seal_sid
```

用一个未满额实例贯穿本节：原服务已接收两个客户端更新，最低人数为三；第三个客户端的更新仍在途，服务节点发生变化。后继服务保留前两个更新，完成状态延续后接收第三个更新，产生同一实例的聚合结果。状态延续等待期间，后继服务还可以处理其他实例。此后再次发生服务变化时，这个实例提供已发布结果和退役记录。

服务为每个实例保存基础模型版本、已纳入更新、当前服务代数、累计受保护聚合状态和阶段。缓冲区决定何时形成一次聚合；经确认且可取得的纳入记录和密文使该缓冲中的计算能够跨服务保留，秘密份额重分享使后继服务具备继续计算的能力。服务代数随每次成功状态延续增加；新实例从第零代开始。

正文只保留三个面向学习过程的状态：

| 状态 | 可执行的工作 | 状态变化依据 |
|---|---|---|
| 活动聚合 | 接收更新，或在服务变化时延续已确认状态 | 达到聚合门槛、完成状态延续或记录放弃 |
| 结果提交 | 固定参与集合，开启并应用聚合结果 | 结果应用记录和清除记录 |
| 终止记录 | 提供已提交结果或放弃依据 | 后续只读 |

安装确认、清除待完成和单条消息拒绝属于实现状态。正文围绕三个学习状态说明机制，接口名与细分状态放在实现章节。

更新保护属于聚合流程的一部分。客户端先取得实例的基础模型、公开保护参数和当前服务信息。

聚合服务验证客户端资格、模型版本、服务代数、更新标识和保护材料有效性。纳入集合与受保护聚合值通过同一经确认的记录更新；重传返回原纳入记录。在途但尚未接受的更新，在服务延续完成后重新核验并提交到同一实例。

首选保护实现沿用既有思路稿的系数域方案：模型增量由 RLWE 聚合保护，客户端密钥系数向量 $k_u$ 由实例阈值公钥 $PK_{sid}$ 加密。记 $x_u$ 为已编码的加权更新，$c_u$ 为受保护更新，$e_u$ 为密钥系数密文。聚合服务维护：

$$
C_{sid}=\bigoplus_{u\in A_{sid}}c_u,\qquad
K_{sid}=\bigoplus_{u\in A_{sid}}e_u,
$$

以及实例秘密 $z_{sid}$ 的阈值份额，满足 $PK_{sid}=z_{sid}G$。最终开启 $K_{sid}$ 得到聚合密钥系数，再用它恢复 $C_{sid}$ 对应的加权更新和。客户端权重总量与编码参数一同纳入结果计算；每份保护载荷须满足后端规定的有效性和取值范围条件。

具体采用有界系数上的阈值 ElGamal。样本权重先用于模型增量编码，再保护该增量；密钥系数按原采样规则生成。记聚合后的第 $j$ 个系数密文为 $(U_j,V_j)$，原授权成员 $i$ 的秘密份额为 $z_i$，其公开验证值为 $X_i=z_iG$。该成员提供：

$$
D_{i,j}=z_iU_j,\qquad
\pi_{i,j}:\ \log_G X_i=\log_{U_j}D_{i,j}.
$$

等离散对数证明采用已有的非交互证明组件，绑定实例的最终开启授权、成员和系数位置。$X_i$ 由当前份额承诺验证。接收方取得同一代的 $\tau_r$ 份有效贡献后，用插值系数 $\lambda_i$ 恢复：

$$
V_j-\sum_i\lambda_iD_{i,j}
=\left(\sum_{u\in A_{sid}}a_{u,j}\right)G.
$$

这里 $a_{u,j}$ 是 RLWE 密钥进入 NTT 前的有界系数，满足 $|a_{u,j}|\le\rho$。实例人数上限为 $k$ 时，在 $[-k\rho,k\rho]$ 内恢复系数和，随后执行 NTT 和聚合解码。这一步使用已交付的贡献及公开参数，原持有者可在交付和清除条件满足后退出。实例解密密钥与服务认证、阈值签名及交接传输密钥分别生成；各组件的公开参数须满足所选群上的加密假设。

这一选择把秘密重分享对象收敛到每个活动实例的一个随机标量，开启通信则随密钥系数维度增长。正文交代密文可加性、整体开启和份额重分享三项作用；群参数、输入有效性与解码开销放在实现章节。真实组件的兼容性与成本由组件实验检验。

#### 3.2.2 Continuing an in-flight aggregation

状态延续从固定训练输入开始。对仍在收集更新的实例，原服务在交接决定中固定 $A_{sid}$ 及其对应的累计聚合状态，随后停止该实例的新纳入。冻结前已经确认的更新都包含在延续输入中；客户端持有的纳入回执可以用于核对这组更新。对已有开启授权的实例，交接沿用其最终参与集合及授权，按第 3.2.3 节传送开启材料。交接决定与结果提交由同一记录顺序确定；先已提交的实例传送固定结果。

状态延续记录绑定 $sid$、基础模型版本、原服务与后继服务、服务代数、已纳入更新及其累计聚合状态。后继服务验证这些绑定关系，对实例密钥份额执行可验证重分享，并安装后继状态：

$$
\{[z_{sid}]_{r,i}\}_{i\in \mathcal A_r}
\xrightarrow{\mathsf{Reshare}}
\{[z_{sid}]_{r+1,j}\}_{j\in \mathcal A_{r+1}},
\qquad PK'_{sid}=PK_{sid},
$$

$$
(v'_{sid},A'_{sid},C'_{sid},K'_{sid})
=(v_{sid},A_{sid},C_{sid},K_{sid}),\qquad g'=g+1.
$$

模型维度、每份更新的权重、权重总和与编码参数也保持一致。这一等式描述交接时刻；激活后的后继服务可继续扩大 $A_{sid}$，同步累加密文和权重。后继安装确认要求其已经取得并验证冻结前缀对应的保护载荷和新份额。原服务依据该确认完成本代秘密份额和恢复材料的清除；在规定清除假设下验证退役依据后，后继服务才恢复写入。同一延续记录对应唯一后继状态，重复消息沿用该状态。退役回执记录协议事件，安全清除仍依赖诚实执行及故障恢复假设。

这一顺序允许原服务与后继服务的成员完全不相交，同时把计算等待限制在发生状态延续的实例。新服务完成初始化后即可接收新实例；原服务继续履行已有实例的交接或开启职责，完成相应义务后逐步退出。为使某个实例最终完成，成员变化调度需允许它完成必要的协议步骤并获得足够客户端更新。连续变化若始终抢在这些步骤完成之前，该实例可能持续等待。每一代的前缀和退役依据都可追溯。

密钥变化在这里说明清楚：服务节点身份密钥按认证策略维护，服务认证材料适应新的节点集合；存量实例保持 $PK_{sid}$ 和已有密文，刷新秘密份额；新实例生成独立密钥。客户端加入或退出改变提交资格。旧代数下尚未纳入的更新可在同一实例恢复后重新绑定当前提交上下文；是否复用密文由保护接口的绑定方式决定，已经纳入的密文直接保留。

状态延续与结果提交的核心伪代码如下。`CertifiedTransfer` 从服务变更最终记录取得实例的交接决定，固定已确认状态及后继权限；`Snapshot` 取得该决定对应的完整载荷。等待期间保留已经完成的步骤，恢复执行沿用同一实例与决定。`AgreeCurrent` 通过当前授权服务的一致协议确认操作，按实例状态核验交接、开启、提交和放弃的先后关系。

```text
Algorithm 1: Continue(sid, old, new)
Input: instance sid, source old, certified successor new
decision <- await CertifiedTransfer(sid, old, new)
R_0 <- await Snapshot(decision)
if R_0.status in {committed, sealed, abandoned} then
    return await VerifiedCopy(new, R_0, decision)
if R_0.status = opening then
    cert <- R_0.open_certificate
    partials <- await ValidDistinctShares(cert, cert.threshold)
    receipt <- await VerifiedOpeningInstall(new, R_0, cert, partials, decision)
    schedule RetireSourceShares(old, sid, receipt)
    return receipt
R_1 <- await VerifiedInstall(new, Reshare(old, new, R_0), R_0)
ret <- await VerifiedRetire(old, R_0, R_1)
Activate(R_1, ret)
return R_1

Algorithm 2: Commit(sid)
R_sid <- await CurrentRecord(sid)
if R_sid.status = active then
    require |R_sid.A| >= k
    cert <- AgreeCurrent(Open, sid, R_sid.v, R_sid.A, R_sid.aggregate,
                         R_sid.g, R_sid.owner, R_sid.threshold, R_sid.encoding)
else
    require R_sid.status = opening
    cert <- R_sid.open_certificate
partials <- await ValidDistinctShares(cert, cert.threshold)
y <- RecoverAggregate(R_sid, Combine(partials, cert))
commit <- AgreeCurrent(Commit, sid, cert, y, NextModelPosition)
AtomicApplyOnce(commit)
schedule RetireRemainingShares(sid)
upon VerifiedAllRetirements(sid, ret): Record(Seal, sid, commit, ret)
return commit
```

`AgreeCurrent(Open)` 确认后使实例进入 `opening`；`AgreeCurrent(Commit)` 确认后使其进入 `committed`，并返回唯一应用位置。服务更替期间，提交请求可由后继继续处理，已有提交返回原记录。`ValidDistinctShares` 可以使用已保存并通过验证的旧成员贡献；`RecoverAggregate` 使用聚合密钥系数、密文、权重和编码参数恢复加权平均。`VerifiedOpeningInstall` 要求后继取得完整开启材料并满足保存条件，随后秘密份额可以按清除规则退役。可用于合并的贡献和证明持续保存到结果及其验证依据可供恢复。

#### 3.2.3 Committing results and handling failures

实例达到人数条件后，聚合服务固定最终参与集合、受保护聚合值、模型版本与编码参数，生成整体开启证书。证书还绑定服务代数、当前服务组及开启门槛。成员只为证书指定的聚合状态提供部分开启；合并要求足够多的不同成员，并核验其结果。实例在等待部分开启时保持 `opening`，最终集合固定。

合并成功后发布：

$$
Commit_{sid}=(sid,v_{sid},A_{sid},Y_{sid},cert_{sid},t_{sid}),
$$

其中 $t_{sid}$ 是结果在模型更新序列中的位置，在提交结果时由一致记录确定。缓冲满额与开启等待期间保持该位置未分配，使其他已完成实例能够先推进模型。模型服务按该位置使用第 3.1.1 节的更新式，将新模型与已应用实例标记原子保存；恢复时从同一已确认位置继续执行。服务变化和重复交付沿用同一结果与应用位置。

开启授权固定份额持有者及其验证参数。服务更替期间，这些成员继续生成同一授权下的部分开启；后继服务可收集、验证和合并其贡献，并依据已承接的发布权限确认结果。所选开启组件须支持贡献的公开验证，并允许接收方仅凭有效贡献及公开参数完成合并；涉及后续交互的组件应将相应响应一并计入交付条件。开启材料包括最终参与集合、基础模型版本、密文、权重及编码参数、开启证书，以及至少 $\tau_r$ 份不同成员的有效贡献和证明。后继取得这些材料并满足可用性条件后，恢复该聚合值所需的秘密运算已经完成。

这一处理保持已有开启证书和份额代数。后继对结果的确认使用当前成员的一致协议；若结果在交接决定之前已提交，则直接继承原结果与应用位置。原服务最终记录确定两种情况，后继据此处理迟到的结果消息。旧成员继续参与该实例的重分享或开启职责由记录决定，后续结果投票则由当前授权服务承担。

秘密份额的清除可以在最终聚合已经可恢复、其开启材料由授权接收方可靠保存后进行。可验证的部分开启、相应密文和结果依据继续保存，直到结果记录足以支持后继恢复。先前各代退役依据、当前清除条件和 $Commit_{sid}$ 齐备后，生成 $Seal_{sid}$。因此，结果等待发布时，旧节点可以已经完成秘密清除；结果已用于训练而清除尚待完成时，由原持有者继续退役。

**两个并发聚合的处理。** 令 $sid_a$ 已接收两个更新、还需第三个更新，$sid_b$ 已固定三个参与者并获得开启授权。后继服务沿用 $sid_a$ 的输入前缀，通过重分享取得新份额；$sid_b$ 则沿用原开启证书，取得足够可验证的贡献。两条路径满足：

$$
g'_a=g_a+1,\qquad g'_b=g_b,\qquad
\bigl|\mathsf{ValidShares}(sid_b,g_b,cert_b)\bigr|\ge\tau_r.
$$

旧节点在完成服务记录交接、$sid_a$ 的重分享与清除、$sid_b$ 的开启材料交付与清除，以及所选协议的剩余职责后退出。此后 $sid_a$ 的第三个客户端仍可到达，$sid_b$ 的结果由后继服务确认和应用。若新组再次变化，尚待发布的开启材料随其当前记录继续传送。各阶段都按实际可取得的材料判断进度。

这一路径将旧节点需要完成的工作限定为已有计算的交付与退役。客户端等待和模型发布继续由后继处理。相关状态与消息的完整推演见研究追踪 P145，正文用这一双实例场景说明退出条件。

故障处理与代价属于结果提交的收尾部分。

恢复节点先验证当前服务链及实例决定，再恢复其有资格持有的状态。仍在收集更新的实例，状态延续确认前由原服务承担恢复；确认后、原服务退役前，后继保持已安装状态并等待；激活后由后继继续纳入。已有开启授权的实例，后继从已保存的证书、密文和贡献恢复，缺少贡献时继续向原授权持有者收集。已提交实例读取固定结果，未完成清除者继续退役。已封存实例提供结果记录。

缺少安装确认或退役依据时，实例保留当前阶段并重试。首版允许在最终开启授权之前，通过与冻结及开启互斥的一致决定放弃实例。超时产生放弃提议；足以排除原服务未来提交的决定才构成 `abandoned`。若源前缀尚未确认或无法取得排他决定，实例继续等待。这样客户端重提依据一个已经终止的原实例，避免同一计算同时进入两个结果。

重提保留原更新的来源标识，并生成指向新实例的提交记录。客户端资格和模型版本仍适用时，可以复用本地计算结果，按新实例密钥重新保护；需要更新基础模型时重新训练。已放弃实例保留终止记录，残留秘密材料继续依照清除与暴露约束处理。

代价按活动实例、待清除实例和历史结果分别记账。设 $L$ 为仍需状态延续的实例数，$U$ 为结果已固定但尚待退役的实例数，$H$ 为历史结果数，则：

$$
S_{total}=S_{live}(L,d,m,n)+S_{pending}(U)
          +S_{results}(H)+S_{admission},
$$

$$
Comm_{continuation}=\sum_{sid\in\mathcal L}
\bigl(Comm_{state}(sid)+Comm_{reshare}(sid)+Comm_{retire}(sid)\bigr)
+Comm_{opening}+Comm_{records}.
$$

 $d$ 为更新维度，$m$ 为保护密钥系数维度，$n$ 为服务组规模。$Comm_{opening}$ 计入已授权实例的证书、密文、开启贡献、验证材料及保存确认的传送。选择性状态延续把秘密重分享集中在仍需继续纳入的实例；参与信息、结果存储、后继追赶和清除等待分别计入总开销。实验据此测量已确认客户端计算的保留率、旧节点在线时长、模型推进及实际通信量。

本节结束时，读者应能够沿贯穿示例解释三个决定：第三个客户端何时进入原实例，后继服务何时取得写入资格，以及结果何时用于训练。状态记录保证输入前缀、唯一后继和提交结果的一致性；真实训练接入与保护组件测量在实现及评价章节提供证据。

**Pseudocode placement.**

正文只保留两个正式算法，不为 `opening`、`committed`、`sealed` 等内部阶段分别编写伪代码。状态表说明阶段语义，算法说明更新如何进入聚合、状态如何延续以及结果如何提交。这样可以把伪代码集中在训练机制上。

两个算法分别解释在途聚合如何跨服务继续，以及聚合结果如何固定并应用。底层的重试、记录序列化、消息去重和密码学库调用不在正文伪代码中展开；这些内容放入实现章节，用实验测量其开销。






### 3.3 Implementation

介绍事件驱动执行、记录持久化、节点退出与恢复，以及与异步 FL 框架的适配边界。实现使用统一轨迹驱动四类策略，保留客户端更新编码和模型训练流程的一致性。密码学实现只描述完成接口所需的操作和测量方法。

组件方案先按以下分工验证，完整消息表和预算见研究追踪 P146：

| 功能 | 参考组件与需要落实的条件 |
|---|---|
| 更新与聚合密钥保护 | Buffalo 的 RLWE 路径及系数域阈值 ElGamal；检查系数导出、聚合解码及公开可验证的部分开启 |
| 活动实例重分享 | 以 DyCAPS 的移动敌手分析和旧组提前完成发送的结构为参考，核对初始份额就绪、私密通信、清除和后继恢复条件 |
| 成员退出后的消息取得 | 复用保护载荷的保存机制，保存完整、按接收者加密的必要交接消息；发送者依据保存确认结束相应交付职责 |
| 进一步减少恢复成本 | 研究 Optimistic DPSS 的可证明分享及后继内部恢复，结合 LightSecAgg 的预处理思路；按其各自敌手模型分析扩展 |

DyCAPS 原协议允许旧节点在发送份额转换材料后清除并离线，后继随后完成刷新；它依赖已发送消息最终交付，并要求前次分享或交接已使每个诚实旧成员取得有效份额。本文允许退出时丢失在途消息，因此进一步研究由后继保存加密交接消息的方式。保存者只保管密文，指定接收者取得材料后完成原协议验证。完整批次的保存确认说明消息有副本；其中求值及证明是否有效，仍由重分享组件核验。接收密钥保留到相应输入被处理且组件恢复条件满足，再与中间状态一起清除。

消息保存还承担一段持续的交付职责。在 $n'=3f'+1$ 的参考情形中，$n'-f'$ 个实际保存确认给出至少 $f'+1$ 个诚实副本，前提是故障上限覆盖这段保存期间。接收者恢复时仍须能联系到至少一个持有完整批次的诚实保存者。若保存者也要退出，应先把交付职责和材料传给后继；持续换人时的可用性依据这一传递过程判断。

P147 的可执行检查验证了发送进程结束后的两种恢复：保留私钥的接收者从副本中恢复有签名的交接载荷，独立进程从公开密文及三份贡献中恢复系数和。P148 随后接入原版 DPSS：旧组保存真实 Prepare、Reduce 消息并结束进程，后继独立完成输入验证、刷新和份额分发，沿同一公钥追加第三个客户端的 BLS12-381 系数密文。所有六种两人组合恢复相同系数和，低于输入门槛的接收者补齐消息后完成。

当前原型使用原组件的可信初始化方、公开测试 KZG 设置及组内进程通道，检验范围是功能接合。原 Reduce 证明已执行验证；用于最终开启的等离散对数证明、私密消息保存、RLWE 模型解码和实际网络训练继续接入。实现与日志见 `experiments/REAL_COMPONENT_BRINGUP.md`。

P145 的算法先采用后继实际安装完成后的退出条件。提前退出优化将旧节点完成交付与后继完成计算区分开：旧节点在必要消息已经可取得、后续步骤全部由后继承担时结束该实例职责，实例则在新份额和退役依据齐备后恢复纳入。这一优化须完成通信实现与移动安全条件的对应核验后，才替换算法中的保守等待。

Optimistic DPSS 的 2026 年完整版按 epoch 开始时选定腐蚀集合，其可证明分享和后继恢复机制可作为后续优化基础。周期内自适应腐蚀下的组合保证继续由对应的安全分析支撑。性能比较分别计入份额传送、保存确认、开启贡献及证明、后继内部通信和模型密文传送；所有字节预算与真实测量分开报告。

### 3.4 Evaluation

先用抽象记录验证实例延续和故障轨迹，再接入真实异步 FL 工作负载。主实验使用同一客户端到达、网络延迟和服务变化轨迹，比较：

```text
Static
Full-transfer
Periodic-rekey
Selective-finality
```

实验围绕四个系统假设组织：

- **H1:** 选择性状态延续在服务变化期间保持更高的活动实例推进能力；
- **H2:** 选择性状态延续以较小的持久记录代价保留已发布实例的结果；
- **H3:** 节点可用性变化影响活动实例的状态延续、开启及退役等待，已提交结果保持稳定；
- **H4:** 对相同的已确认输入与模型应用顺序，服务变化执行和参考执行产生相同的聚合结果与模型更新；策略间的纳入差异另行报告。

这些假设由实例完成率、客户端纳入率、模型版本推进、模型质量、状态延续与恢复延迟、通信量、拒绝消息数和持久记录大小共同检验。

### 3.5 Analysis: consistency, privacy, and convergence

本节计划用简短结果支撑系统设计，具体证明随组件实例化完成：

1. 一个条件性训练连续性结果，说明成功状态延续保留已接受前缀及权重，后继服务沿同一实例追加合格更新；
2. 一个实例结果一致性结果，说明结果提交后参与集合、聚合结果和模型版本保持不变；
3. 一个受限移动敌手条件下的安全聚合隐私结果，结合累计暴露上限、状态延续转录安全和份额清除，说明单个诚实客户端更新不会因服务更替而额外泄露；
4. 一个训练轨迹保持结论：给定相同的纳入更新、权重、基础版本、编码与解码规则、结果应用顺序及学习随机性，成功状态延续保持参考执行的聚合结果和模型轨迹。进一步的收敛结论需要核对所用异步学习算法的光滑性、方差、抽样分布与陈旧度条件，再单列量化误差和成员变化导致的参与偏差。异步通信假设本身给出消息交付条件；有界陈旧度与墙上时间收敛率需要额外分析。
本文提供的是安全聚合意义上的密码学隐私：聚合服务可以得到聚合结果，但无法从保护载荷中恢复单个诚实客户端更新。本文不把差分隐私、最终模型推断防护或新的加密原语作为贡献；隐私保证服务于动态聚合系统，主体贡献仍是服务变化下的训练连续性和结果一致性。底层保护组件、主动重分享和受限移动敌手的组合证明放入附录，正文只解释它们如何支撑上述结果。

底层原语证明、状态访问结构和组合假设放入附录。正文中的结果都对应训练连续性、模型推进或聚合结果稳定性。

### 3.6 Related work

按服务问题组织相关工作，而不是按密码学原语罗列：

- 异步和缓冲 FL：客户端到达与模型推进；
- 异步安全聚合：客户端更新保护与聚合释放；
- 动态聚合服务：服务节点变化与退出/恢复；
- VSS/DPSS：秘密状态延续与恢复；
- 阈值加密和前向安全：历史材料处理的不同语义。

每类工作回答三个问题：它如何组织异步聚合实例，如何处理成员变化，是否定义已完成实例的后续处理边界。

本轮文献对照明确以下依据，详细摘录与参数讨论保存在研究追踪 P144，双实例扩展见 P145，具体组件、消息流程及成本见 P146：

| 工作 | 已有机制及其与本文的关系 |
|---|---|
| [FedBuff，AISTATS 2022](https://proceedings.mlr.press/v151/nguyen22b.html) | 缓冲式异步训练及收敛分析，是训练流程与学习指标的参照 |
| [LightSecAgg，MLSys 2022](https://arxiv.org/html/2109.14236v3)，第 3、4、6 节 | 用编码恢复活动用户的聚合掩码，明确权衡隐私门限、恢复人数和掉线数；本文借鉴聚合恢复与预处理，研究可在退出前完成的计算 |
| [Flamingo，S&P 2023](https://eprint.iacr.org/2023/486)，第 4.6 节、附录 B.4 | 已支持解密委员会轮换、保持公钥的秘密重分享，并讨论跨更换累积份额的风险；本文以此为基础，研究异步在途聚合的迁移及结果应用 |
| Buffalo，CODASPY 2025，第 5 节 | 异步缓冲、参与集合一致性检查、门限恢复聚合密钥；为开启条件和聚合保护提供参照 |
| Willow，第 2.1、6.7 节；Setup Once, Secure Always，ASIACCS 2026 | 分别研究单次提交客户端与辅助委员会、跨训练轮的动态用户；据此区分训练客户端参与和服务节点的份额持有职责 |
| Privacy-Preserving Federated Averaging with Byzantine Aggregators in Asynchronous Networks，第 4 节 | 在固定聚合者集合和可靠异步消息条件下分析故障与训练偏差，为本文的通信条件和学习分析提供直接参照 |
| [DyCAPS](https://eprint.iacr.org/2022/1169)；[CHURP，CCS 2019](https://eprint.iacr.org/2019/017) | 支持动态持有者的秘密状态延续；选择组件时逐项核对网络、主动腐蚀、清除和门限假设 |
| [Optimistic Asynchronous DPSS，S&P 2026](https://eprint.iacr.org/2025/880)，第 4.1 节及算法 1、3 | 旧成员取得分享证明并转交后，后继选择有效分享、恢复缺失份额；为缩短旧成员参与时间及降低恢复成本提供基础 |

当前方案按聚合实例完成退出职责：已授权开启的实例交付绑定最终集合的开启材料及结果确认依据，仍需接收更新的实例完成状态延续，后继服务承接结果确认并并行承担新训练。组件核对已明确旧成员发送完成、材料可取得和后继计算完成三个不同事件。P147 完成材料恢复检查，P148 接通真实 DPSS 与跨交接的系数聚合。后续连接私密消息保存、可验证开启和训练负载，测量退出时点与后继就绪之间的等待。改进价值由实例完成率、旧节点所需在线时长和训练吞吐量验证。

具体从 Flamingo 的轮换机制出发，将“每隔若干轮转移长期密钥份额”扩展为“依据并发聚合的计算进度安排退出”。LightSecAgg 的编码恢复与训练并行预处理则启发另一项优化：把与最终参与集合无关的准备工作提前完成，在集合确定后只执行必要的聚合恢复。尚未确定集合的实例仍需保留可追加更新的受保护状态。首版沿用当前阈值保护后端，先检验按实例安排交接和开启的收益；编码方案作为进一步的恢复优化候选，具体采用前核对主动敌手、跨代暴露和状态绑定条件。Flamingo 对委员会更换的讨论与其正式静态敌手证明分别引用，本文的受限移动敌手保证由所选主动重分享组件及组合分析支撑。

## 4. Evaluation map

### 4.1 Scenarios

`S0` 到 `S3` 建立正常训练和常见故障行为，`S4` 到 `S6` 检查迟到消息与封存边界，`S7` 和 `S8` 作为动态成员主场景：

```text
S7: active-instance join/leave and state continuation
S8: multiple service changes with concurrent instances
```

### 4.2 Figures

1. 一次服务变化中多个聚合实例的状态时间线；
2. 实例完成率和客户端纳入率随服务节点 churn 的变化；
3. 模型推进速度和模型质量随完成聚合数的变化；
4. 状态延续与恢复延迟随并发实例数的变化；
5. 持久记录大小和通信量随服务变化次数的变化；
6. 迟到消息拒绝数与聚合结果一致性。

### 4.3 Interpretation

论文把模型质量与系统代价分开报告。模型质量使用完成聚合数作为横轴，避免把服务等待时间误读为训练质量。状态延续延迟、记录大小和客户端纳入率解释选择性状态延续的服务代价。固定服务提供性能参照，全量状态迁移和周期重建说明替代策略的具体取舍，Buffalo、Flower SecAgg+ 和 FLSim 提供开源 FL 对照。

## 5. Scope and paper decisions

首篇论文固定唯一后继服务链、多个并发聚合实例以及服务节点加入和退出，允许连续多次服务变化。节点暂时失联与恢复作为可用性事件处理。系统使用已有异步一致协议确认服务记录与实例决定，研究重点放在活动聚合的计算保留、条件性训练推进和结果提交。

论文把以下条件作为系统边界：聚合实例记录有明确版本，服务变化记录可验证，节点恢复读取最新记录，已提交实例只产生读取结果。任意成员任意时刻变化、通用动态密码学基础设施和新的聚合原语留给后续工作。

稿件进入实现评估阶段的判断标准是：读者在看到安全性章节前，已经理解聚合实例为什么需要转移、为什么需要确定最终状态，以及这些决定如何影响训练连续性和模型质量。
